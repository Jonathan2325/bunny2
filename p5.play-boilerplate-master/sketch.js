// Sorry for submitting blank, it seems like the files i downloaded were blank with no image files in it
function setup() {
  createCanvas(800,400);
  createSprite(400, 200, 50, 50);
}

function draw() {
  background(255,255,255);  
  drawSprites();
}